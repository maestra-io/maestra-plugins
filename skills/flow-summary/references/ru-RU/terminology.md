<!-- locale: ru-RU. Parallel variant of ../en-US/terminology.md. Keep both in step. -->

# Flow summary — terminology (ru-RU)

Per-locale wording. Keys are stable; only the wording column differs.

## Section headings

| key | wording |
|---|---|
| `heading.trigger` | `### Стартовое событие` |
| `heading.logic` | `### Логика сценария` |
| `heading.goal` | `### Цель сценария` |

## Fixed labels

| key | wording |
|---|---|
| `label.unresolved_block` | Поведение блока подтвердить не удалось |
| `label.no_run_data` | За этот период данных о запусках нет |

Block output names (`:default`, `:positive`, `:negative`, `:beforeLimit`, `:afterLimit`,
`:1(75%)`) and block type tags are values the flow tools return — quote them verbatim in
any locale, never translate them.
