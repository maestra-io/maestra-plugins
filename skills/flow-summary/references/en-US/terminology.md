<!-- locale: en-US. Parallel variant of ../ru-RU/terminology.md. Keep both in step. -->

# Flow summary — terminology (en-US)

Per-locale wording. Keys are stable; only the wording column differs.

## Section headings

| key | wording |
|---|---|
| `heading.trigger` | `### Trigger` |
| `heading.logic` | `### Flow logic` |
| `heading.goal` | `### Goal` |

## Fixed labels

| key | wording |
|---|---|
| `label.unresolved_block` | Behaviour could not be confirmed |
| `label.no_run_data` | No run data for this period |

Block output names (`:default`, `:positive`, `:negative`, `:beforeLimit`, `:afterLimit`,
`:1(75%)`) and block type tags are values the flow tools return — quote them verbatim in
any locale, never translate them.
